<a href="/donations/new">New Donation</a>

{{!-- {{#each charity}}
    <h1>{{this.name}}</h1>
    <h2>{{this.description}}</h2>
{{/each}} --}}

{{#each donation}}
    <h1>{{this.name}}</h1>
    <h2>{{this.message}}</h2>
    <h3>${{this.donationAmount}}</h3>
{{/each}}